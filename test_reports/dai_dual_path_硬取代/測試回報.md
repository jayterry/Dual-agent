# dai_dual_path_硬取代 — 測試回報

| 項目 | 內容 |
|------|------|
| 日期 | 2026-08-04 |
| 狀態 | **sandbox_passed**（pytest + 審計 0 fail；待你確認後再 git） |
| 對應對話 | `test_dialogue/dai_dual_path_硬取代/` |
| 關聯 | 桌面 `ML` 雙路計劃；`docs/VERIFICATION_WORKFLOW.md` |

## 問題摘要與使用者影響

DAI 主路徑仍為舊 SMS 規則 DAG + 權重／銀行 LR 融合，與教授方向的「個人化雙路（Threat+Context 並列、禁止數值融合、Path B 純 LLM）」不一致。需**硬取代**主產品路徑，並在桌面顯示雙路面板。

## 根因

- `SMS_REVIEW_DAG` 固定多步規則融合
- 桌面 ML 套件未接入 Dual-agent
- `review_display`／桌面僅單一 `risk_score`

## 修正策略

1. Vendor `Desktop/ML` → `dual_agent/dai/fraud_dual` + `data/fraud_dual_models/`
2. 硬切 `SMS_REVIEW_DAG = ("dual_path_analyze",)`
3. 適配層 `risk_analysis/dual_path.py`：persona → `run_pipeline` → report（path_a／path_b 各含分數+原因+警示）
4. CAI 相容：`gate = max(threat, context)` → `risk_score`／`verdict`／`recommended_cai_action`
5. Narrator 走同一組 DAI Ollama
6. 桌面建圖欄位 + 雙路摘要顯示
7. 舊規則／ml_lr 模組保留離線，但**不再**為產品主路徑

## 劇本設計

| group | 用途 |
|-------|------|
| baseline | OTP／帳密詐騙 → Path A 有分數與原因；gate warn/block |
| experimental | 高齡+投資話術 → Context 相對抬高；良性通知偏 allow |
| control | Path B 關閉時仍可產出 Path A；report.engine=`fraud_dual` |

## 驗收標準

1. `pytest tests/test_fraud_dual_path.py` 及既有不依賴舊管線之測試通過
2. `run_problem_reports_audit.py` 本問題 **0 fail**
3. 桌面可見 Path A／Path B 區塊（Path B 無 Ollama 時標示略過）

## 建議修復（實作清單）

- [x] fraud_dual 套件 + 模型
- [x] dual_path 適配 + DAG 硬切
- [x] reporting／call_dai／desktop
- [x] scenarios + pytest + 審計
- [x] docs/RISK_SCORING.md 註記主路徑變更

## 沙盒／驗收結果（2026-08-04）

| 檢查 | 結果 |
|------|------|
| pytest（相關套件） | **55 passed** |
| 審計 fail 數 | **0 fail / 0 warn**（含 `dai_dual_path_硬取代`） |
| Path A OTP 煙測 | Threat≈77、OTP_Scam、gate=warn |
| 對話連結 | `test_dialogue/dai_dual_path_硬取代/2026-08-04_Cursor沙盒.md` |

### 工作流位置

- 階段 1–3：已完成（沙盒通過）
- 階段 4 上 git：**尚未做**（等你確認後再 commit／push）
