# 次要發現（同次審計 2026-07-11）

與 `pending_review_無法取消` 同次執行 `run_problem_reports_audit.py` 時一併觀察；若需獨立追蹤，另建問題資料夾與 `scenarios.json`。

| # | 嚴重度 | 問題 | 備註 |
|---|--------|------|------|
| 1 | 中 | 單字「你」未命中 `meta_assistant_or_chat_scope` | Planner 可能誤排 `search_web`；需擴充短路或 validate |
| 3 | 低 | semantic_router `confidence=low` 等交回 LLM | 需 Ollama E2E；若成為獨立問題再建 `test_reports/<問題>/scenarios.json` |
| 3 | 環境 | 審計時 Ollama 未運行 | 無法做真實 LLM 端到端；`ollama serve` 後補測 |
| 4 | 資訊 | Ingress：「我沒有要你看簡訊」→ `itt=unknown`，無 `review_pending_candidate` | 可接受；問題在 **session 狀態** 未清，非 Ingress 誤判 |

meta 搜尋誤觸若再現，建獨立問題資料夾（如 `meta_誤觸搜尋`）並從對話摘劇本，勿併入無關問題的審計。
