# 執行依賴：Ollama、LangChain、FastAPI 等（見各套件版本下限）。
# 安裝：在 Dual-agent 目錄執行  pip install -r requirements.txt

fastapi>=0.115.0
uvicorn[standard]>=0.30.0
langchain-core>=0.3.0
langchain-ollama>=0.2.0
langchain-chroma>=0.2.0
chromadb>=0.6.0
pydantic>=2.7.0
pytest>=8.0.0
